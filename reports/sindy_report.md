# SINDy Report — C. elegans 4-Cell Stage Dynamics

> **Date generated**: auto-generated by sindy_analysis.py (Phase 4)
> **Model**: SINDy with STLSQ (threshold=0.05, α=0.05)
> **Library**: 13 candidate terms (linear, quadratic-position, cross, inverse)

---

## 1. DATA SUMMARY

### 1.1 ABM Trajectory (Dataset A)

- Source file: `simulation_results.pt` → key `trajectory_4cell`
- **4-cell equilibration phase**: 51 frames (t = 320 to 820, Δt = 10.0)
- Cells: ['ABa', 'ABp', 'EMS', 'P2']
- Observations: 51 frames × 4 cells = **204 rows**
- Feature matrix shape: `(204, 7)`
- Derivative matrix shape: `(204, 3)`
- Contact area, V0, γ extracted directly from trajectory.

**ABM feature statistics:**

| Feature | Mean | Std | Min | Max |
|---------|------|-----|-----|-----|
| x | 0.7466 | 7.382 | -7.319 | 18.27 |
| y | -0.2051 | 5.125 | -6.719 | 6.632 |
| z | -0.2615 | 4.067 | -5.928 | 5.494 |
| contact_area_sum | 1455 | 499.6 | 470.1 | 2759 |
| V0 | 2424 | 534 | 1616 | 3008 |
| gamma | 0.7836 | 0.06778 | 0.6826 | 0.8455 |
| n_neighbors | 2.235 | 0.6668 | 1 | 3 |

**ABM derivative statistics (μm per simulation time unit):**

| Axis | Mean | Std | Min | Max |
|------|------|-----|-----|-----|
| dx/dt | -0.002998 | 0.01766 | -0.06725 | 0.05 |
| dy/dt | -0.001276 | 0.01096 | -0.04839 | 0.03 |
| dz/dt | -0.0008471 | 0.01592 | -0.05 | 0.04616 |

### 1.2 CShaper Real Trajectory (Dataset B)

- Source file: `datasets/CDSample04.txt` (columns: Cell, Time, Z, X, Y)
- Filtered to 4 target cells: ABa, ABp, EMS, P2

**Timepoints per cell:**

| Cell | Timepoints | Notes |
|------|-----------|-------|
| ABa | 1 | ⚠ Only 1 timepoint — no derivative computable; cell excluded |
| ABp | 1 | ⚠ Only 1 timepoint — no derivative computable; cell excluded |
| EMS | 3 | ⚠ Very sparse |
| P2 | 5 | OK |

- **Total CShaper observations with derivatives**: 8

> **⚠ CRITICAL DATA LIMITATION**: CShaper has only **8 observations** (< 10 threshold). The SINDy library has 13 terms. The system is **underdetermined** — coefficients cannot be reliably estimated. All CShaper SINDy results are flagged as **PRELIMINARY**. See Section 6 (Limitations).

**Note on CShaper features:**
CDSample04.txt contains positions only (X, Y, Z in voxel units). Contact area (`ca`) and neighbor count (`nn`) are **unavailable** and have been set to 0 throughout. V0 and γ were taken from calibrated ABM values (Reports 1 & 2). CShaper positions are in voxel coordinates (not converted to μm), so derivative units differ from ABM.

---

## 2. ABM EQUATIONS

Fit: STLSQ (threshold=0.05, α=0.05). Data: 204 observations from 4-cell equilibration trajectory.

### 2.1  dx/dt

```
dx/dt = -0.032130·gamma +0.010249·nn
```

**R² = 0.1211**

**Fit notes**: Two attempts (Rule 7 retry applied):
  - threshold=0.05: 0 surviving terms, R²=-0.0288
  - threshold=0.01: 2 surviving terms, R²=0.1211

**Surviving terms:**

| Term | Coefficient | Biological interpretation |
|------|-------------|--------------------------|
| `gamma` | -0.032130 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven differential drift between lineages. |
| `nn` | +0.010249 | Number of neighbours (contact area >50 units). Non-zero coeff → crowding / neighbour-count-dependent movement. |

### 2.2  dy/dt

```
dy/dt = 0   (no surviving terms)
```

**R² = -0.0135**

**Fit notes**: Two attempts (Rule 7 retry applied):
  - threshold=0.05: 0 surviving terms, R²=-0.0135
  - threshold=0.01: 0 surviving terms, R²=-0.0135

**No terms survived.** Dynamics along this axis are effectively zero or below the sparsity threshold.

### 2.3  dz/dt

```
dz/dt = +0.034772·gamma -0.012680·nn
```

**R² = 0.2537**

**Fit notes**: Two attempts (Rule 7 retry applied):
  - threshold=0.05: 0 surviving terms, R²=-0.0028
  - threshold=0.01: 2 surviving terms, R²=0.2537

**Surviving terms:**

| Term | Coefficient | Biological interpretation |
|------|-------------|--------------------------|
| `gamma` | +0.034772 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven differential drift between lineages. |
| `nn` | -0.012680 | Number of neighbours (contact area >50 units). Non-zero coeff → crowding / neighbour-count-dependent movement. |

---

## 3. CSHAPER EQUATIONS

> ⚠ **PRELIMINARY RESULTS** — Only 8 observations. The 13-term library is underdetermined. Results should be treated as exploratory only.

Fit: STLSQ (threshold=0.05, α=0.05). Data: 8 observations from EMS + P2 trajectories (ABa and ABp excluded: only 1 timepoint each).

### 3.1  dx/dt

```
dx/dt = -0.710383·x +41.300484·z -0.185639·V0 +80.954881·gamma -0.684711·z^2
```

**R² = 0.7946**

**Fit notes**: Threshold 0.05: 5 surviving terms, R²=0.7946

**Surviving terms:**

| Term | Coefficient | Biological interpretation |
|------|-------------|--------------------------|
| `x` | -0.710383 | Position along AP axis (anterior-posterior). Negative coeff → restoring force toward AP centre. |
| `z` | +41.300484 | Position along LR axis (left-right). Negative coeff → left-right symmetrisation. |
| `V0` | -0.185639 | Equilibrium volume. Non-zero coeff → volume-elasticity-driven differential drift; larger cells move differently from smaller ones. |
| `gamma` | +80.954881 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven differential drift between lineages. |
| `z^2` | -0.684711 | Quadratic LR position. Nonlinear LR constraint. |

### 3.2  dy/dt

```
dy/dt = +1.662466·x +0.005742·y -156.868554·z +0.139350·V0 +1707.903739·gamma +2.691777·z^2
```

**R² = 0.8798**

**Fit notes**: Threshold 0.05: 6 surviving terms, R²=0.8798

**Surviving terms:**

| Term | Coefficient | Biological interpretation |
|------|-------------|--------------------------|
| `x` | +1.662466 | Position along AP axis (anterior-posterior). Negative coeff → restoring force toward AP centre. |
| `y` | +0.005742 | Position along DV axis (dorsal-ventral). Negative coeff → restoring force toward DV centre. |
| `z` | -156.868554 | Position along LR axis (left-right). Negative coeff → left-right symmetrisation. |
| `V0` | +0.139350 | Equilibrium volume. Non-zero coeff → volume-elasticity-driven differential drift; larger cells move differently from smaller ones. |
| `gamma` | +1707.903739 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven differential drift between lineages. |
| `z^2` | +2.691777 | Quadratic LR position. Nonlinear LR constraint. |

### 3.3  dz/dt

```
dz/dt = -0.170451·x +0.031007·y +0.623708·z -0.080981·V0 +286.020435·gamma
```

**R² = 0.8486**

**Fit notes**: Two attempts (Rule 7 retry applied):
  - threshold=0.05: 0 surviving terms, R²=-0.0018
  - threshold=0.01: 5 surviving terms, R²=0.8486

**Surviving terms:**

| Term | Coefficient | Biological interpretation |
|------|-------------|--------------------------|
| `x` | -0.170451 | Position along AP axis (anterior-posterior). Negative coeff → restoring force toward AP centre. |
| `y` | +0.031007 | Position along DV axis (dorsal-ventral). Negative coeff → restoring force toward DV centre. |
| `z` | +0.623708 | Position along LR axis (left-right). Negative coeff → left-right symmetrisation. |
| `V0` | -0.080981 | Equilibrium volume. Non-zero coeff → volume-elasticity-driven differential drift; larger cells move differently from smaller ones. |
| `gamma` | +286.020435 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven differential drift between lineages. |

---

## 4. COMPARISON TABLE

All terms that survive in **either** ABM or CShaper equations are listed. '—' denotes coefficient effectively zero (< 1×10⁻¹⁰) or term absent.

### X-axis  (dx/dt)

| Term | ABM coeff | CShaper coeff | Interpretation |
|------|-----------|---------------|----------------|
| `x` | — | -0.710383 | Position along AP axis (anterior-posterior). Negative coeff → restoring force to |
| `z` | — | +41.300484 | Position along LR axis (left-right). Negative coeff → left-right symmetrisation. |
| `V0` | — | -0.185639 | Equilibrium volume. Non-zero coeff → volume-elasticity-driven differential drift |
| `gamma` | -0.032130 | +80.954881 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven |
| `nn` | +0.010249 | — | Number of neighbours (contact area >50 units). Non-zero coeff → crowding / neigh |
| `z^2` | — | -0.684711 | Quadratic LR position. Nonlinear LR constraint. |

### Y-axis  (dy/dt)

| Term | ABM coeff | CShaper coeff | Interpretation |
|------|-----------|---------------|----------------|
| `x` | — | +1.662466 | Position along AP axis (anterior-posterior). Negative coeff → restoring force to |
| `y` | — | +0.005742 | Position along DV axis (dorsal-ventral). Negative coeff → restoring force toward |
| `z` | — | -156.868554 | Position along LR axis (left-right). Negative coeff → left-right symmetrisation. |
| `V0` | — | +0.139350 | Equilibrium volume. Non-zero coeff → volume-elasticity-driven differential drift |
| `gamma` | — | +1707.903739 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven |
| `z^2` | — | +2.691777 | Quadratic LR position. Nonlinear LR constraint. |

### Z-axis  (dz/dt)

| Term | ABM coeff | CShaper coeff | Interpretation |
|------|-----------|---------------|----------------|
| `x` | — | -0.170451 | Position along AP axis (anterior-posterior). Negative coeff → restoring force to |
| `y` | — | +0.031007 | Position along DV axis (dorsal-ventral). Negative coeff → restoring force toward |
| `z` | — | +0.623708 | Position along LR axis (left-right). Negative coeff → left-right symmetrisation. |
| `V0` | — | -0.080981 | Equilibrium volume. Non-zero coeff → volume-elasticity-driven differential drift |
| `gamma` | +0.034772 | +286.020435 | Cortical tension coefficient (lineage-specific). Non-zero coeff → tension-driven |
| `nn` | -0.012680 | — | Number of neighbours (contact area >50 units). Non-zero coeff → crowding / neigh |

---

## 5. SCIENTIFIC INTERPRETATION

### 5.1 What the ABM equations reveal

The ABM SINDy fit captures the effective force law governing cell movement during the 4-cell equilibration phase. Key findings:

- **Cortical tension (γ) and equilibrium volume (V₀) terms**: Their appearance as independent predictors means that the lineage identity (AB vs EMS vs P) or cell size contributes to the effective drift velocity, even after accounting for position. This may reflect asymmetric division outcomes and differential cortical flow (P2 has an active cortical flow in the ABM).

### 5.2 CShaper equations vs ABM equations

**IMPORTANT CAVEAT**: Direct numerical comparison of coefficients is **not valid** because:
1. ABM positions are in μm; CShaper positions are in voxel units.
2. ABM time is in simulation steps (arbitrary units); CShaper time is in integer acquisition frames.
3. CShaper has only 8 observations — results are unreliable.

What CAN be compared is which **terms survive** (structural comparison):

- **Terms in BOTH** (validated physics): ['gamma']
  These terms appear in both ABM and CShaper dynamics. Their presence in the real embryo validates the ABM's choice of candidate forces.

- **ABM-only terms**: ['nn']
  These terms are discovered in the ABM but absent from CShaper. Possible explanations: (a) CShaper data is too sparse to detect them; (b) the ABM introduces artefactual forces absent in the real embryo.

- **CShaper-only terms**: ['V0', 'x', 'y', 'z', 'z^2']
  These terms appear in the real embryo but not the ABM. This could indicate missing physics in the model — e.g., cytoplasmic streaming, spindle forces, or cortical flows not captured by the energy-minimisation ABM.

### 5.3 What hidden variables might explain the gap?

1. **Cell shape deformation**: The ABM uses rigid spheres. Real cells deform under contact, producing non-spherical contact geometries. Shape deformation variables (e.g., aspect ratio, contact angle) are absent from both datasets and may drive terms not captured here.

2. **Cytoplasmic and cortical flows**: PAR polarity gradients drive directed cortical flows in P2 (included partially in ABM as `alpha`) but internal flows affecting nuclear/centrosome positions are absent.

3. **Osmotic pressure and turgor**: Volume elasticity in the ABM is a penalty term; real cells regulate turgor. This may add a pressure-dependent velocity component.

4. **ABM parameter recalibration issue (Report 3b)**: The 4-cell ABM parameters do not transfer to the 8-cell stage without recalibration, suggesting the effective force law changes with cell density. Only the 4-cell phase is modelled here.

---

## 6. HONEST LIMITATIONS

### 6.1 CShaper Timepoint Density

CDSample04.txt contains only **8 derivative-computable observations** for the 4 target cells in the 4-cell stage:

- **ABa**: 1 timepoints → excluded (no derivative)
- **ABp**: 1 timepoints → excluded (no derivative)
- **EMS**: 3 timepoints → 3 observations
- **P2**: 5 timepoints → 5 observations

With 13 candidate library terms and only 8 observations, the SINDy system is **severely underdetermined** (more unknowns than equations). STLSQ can still produce sparse solutions, but coefficient values are unreliable. Any surviving CShaper term should be treated as a hypothesis to test with more data, not a confirmed result.

### 6.2 SINDy Autonomy Assumption

SINDy assumes the dynamics are **autonomous**: dx/dt = f(x) with no explicit time dependence. This is violated if:
- external forcing (division events, PAR polarity) changes during the window;
- the equilibration is transient (non-stationary attractor).
The 4-cell equilibration phase starts immediately after P1 division and ends at steady state — the early frames are transient. Including them may cause non-zero slow-time-varying terms to be misattributed to spatial features.

### 6.3 Spherical Cell Approximation

The ABM uses JKR contact mechanics between spheres. Real C. elegans blastomeres are roughly spherical at 4-cell stage but undergo visible flattening at contacts. The true contact area is therefore larger than JKR predicts (consistent with R² = 0.38 in the calibration). SINDy terms involving `ca` (contact area sum) may be systematically underestimated in the ABM relative to the real embryo.

### 6.4 ABM 4-Cell Topology Instability

Report 2 noted that only 10/20 ABM validation runs produce the correct 3+1 diamond topology. The trajectory used here (a single run with the calibrated parameters) may represent one of these correct runs. SINDy results derived from this single trajectory may not generalise across all 20 runs.

### 6.5 ABM Near-Equilibrium Dynamics

The 4-cell phase spans t=320–820 (Report 3 frame mapping). By t~500+, the system is near equilibrium. Near-equilibrium derivatives are close to zero, which: (a) reduces the effective signal-to-noise for SINDy; (b) may cause sparsity to zero out real physical terms. Results at threshold=0.05 may be overly sparse.

### 6.6 Unit Mismatch (ABM vs CShaper)

- ABM positions: μm. CShaper positions: voxel coordinates (~0.09 μm/px in XY, ~1 μm/px in Z).
- ABM time: simulation gradient-descent units. CShaper time: acquisition frames (~1.5 min each estimated).
- ABM contact areas: μm². CShaper contact areas: unavailable (set to 0).
Coefficient values between ABM and CShaper equations are **not directly comparable**.

---

*Report generated by `sindy_analysis.py`. All source data files read-only — no existing files modified.*